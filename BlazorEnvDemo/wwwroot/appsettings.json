{
  "AppConfig": {
    "EnvironmentLabel": "BASE — this file only loads if no env-specific file overrides it",
    "ApiBaseUrl": "https://api.example.com",
    "EnableDebugPanel": false,
    "BannerColor": "#6c757d"
  }
}
